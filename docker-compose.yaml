version: "3.8"

services:
  app:
    build: .
    container_name: mock-api
    depends_on:
      - db
    env_file:
      - .env
    ports:
      - "8080:8080"
    networks:
      - flexbet

  db:
    image: postgres:17
    container_name: postgres
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - flexbet

networks:
  flexbet:

volumes:
  pg_data: